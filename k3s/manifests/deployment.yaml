apiVersion: apps/v1
kind: Deployment
metadata:
  name: aceest-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aceest
  template:
    metadata:
      labels:
        app: aceest
    spec:
      imagePullSecrets:
        - name: ecr-secret
      containers:
      - name: aceest
        image: image: kollanagamanasa/aceest-app:v2
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
